language: python

python:
  - "3.6"

services:
  - docker

install:
  - pip install -r app/requirements.txt

script: 
  # - docker-compose run my_sanic_app sh -c "pytest && black ."
  - pytest && black .